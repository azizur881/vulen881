import React, { useContext } from 'react';
import { useParams } from 'react-router-dom';
import { UserContext } from '../../App';
import data from '../../fakeData/users'

const Item = () => {
        return (
        <div>
        <h1>Total Item You Have Selected</h1>
        </div>
    );
};

export default Item;